language: node_js
node_js:
- 12
notifications:
  email:
    on_success: never
    on_failure: always
dist: xenial  
addons:
  chrome: stable
before_install:
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=3000 http://localhost &